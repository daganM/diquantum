<%= form_for(@notion) do |f| %>
  <% if @notion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notion.errors.count, "error") %> prohibited this notion from being saved:</h2>

      <ul>
      <% @notion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
