<p id="notice"><%= notice %></p>

  <h1><%= @article.title %></h1>
  <% @article.notions.each do |notion| %>
   <a href="<%= notion_path(notion) %>" class="btn btn-default">
     <%= notion.title %>
   </a>
  <% end %>
<p>
  <%= @article.content %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
